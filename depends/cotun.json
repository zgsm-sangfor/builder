{
  "name": "cotun",
  "type": "exec",
  "version": "1.3.13",
  "repo": "zgsm",
  "path": "../../services/cotun",
  "command": "docker build --build-arg VERSION={{.version}} . -t {{.repo}}/{{.name}}:v{{.version}}",
  "tag": "v{{.version}}",
  "component": {
    "workdir": "./configures/common/cotund",
    "command": "echo IMAGE_COTUN={{.repo}}/{{.name}}:v{{.version}} > ./image.env"
  },
  "enabled": false,
  "description": "Stream transfer tools"
}
