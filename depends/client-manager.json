{
  "name": "client-manager",
  "type": "docker",
  "version": "1.0.14",
  "remote": "git@github.com:zgsm-ai/client-manager.git",
  "repo": "zgsm",
  "path": "../services/client-manager",
  "tag": "v{{.version}}",
  "build": {
    "command": "docker build --build-arg VERSION={{.version}} . -t {{.repo}}/{{.name}}:v{{.version}}"
  },
  "pull": {
    "command": "docker-pull-via.sh --proxy-id ghcr.io/zgsm-sangfor/{{.name}}:{{.tag}} --image-id  {{.repo}}/{{.name}}:v{{.version}}"
  },
  "update": {
    "workdir": "./configures/common/client-manager",
    "command": "echo IMAGE_CLIENT_MANAGER={{.repo}}/{{.name}}:v{{.version}} > ./image.env"
  },
  "description": ""
}
