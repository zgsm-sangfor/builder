{
  "name": "code-completions-go",
  "repo": "zgsm",
  "version": "1.0.0",
  "path": "../../services/code-completions-go",
  "command": "docker build --build-arg VERSION={{ .version }} . -t {{ .repo }}/{{ .name }}:{{ .version }}",
  "tag": "{{ .version }}",
  "description": "Go code completion service"
}
