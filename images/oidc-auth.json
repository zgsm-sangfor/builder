{
  "name": "oidc-auth",
  "repo": "zgsm",
  "version": "1.0.5",
  "path": "../../zgsm-ai/oidc-auth",
  "command": "docker build --build-arg VERSION={{ .version }} . -t {{ .repo }}/{{ .name }}:{{ .version }}",
  "tag": "{{ .version }}",
  "description": "OIDC Authentication Service"
}