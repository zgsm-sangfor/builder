{
  "name": "one-api",
  "repo": "zgsm",
  "version": "1.0.4",
  "path": "../../zgsm-ai/one-api",
  "command": "docker build --build-arg VERSION={{ .version }} . -t {{ .repo }}/{{ .name }}:{{ .version }}",
  "tag": "{{ .version }}",
  "description": "LLM Management Gateway Service"
}