services:
  new-api:
    image: ${IMAGE_NEW_API}
    restart: always
    environment:
      TZ: "Asia/Shanghai"
    volumes:
      - ${COSTRICT_DATA_DIR}/backend/new-api/data:/data
    ports:
      - "${PORT_NEW_API}:3000"
    networks:
      - shenma