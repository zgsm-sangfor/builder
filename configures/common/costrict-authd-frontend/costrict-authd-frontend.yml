services:
  costrict-authd-frontend:
    image: ${IMAGE_COSTRICT_AUTHD_FRONTEND}
    restart: always
    environment:
      - TZ=Asia/Shanghai
    volumes:
      - ${COSTRICT_DATA_DIR}/backend/costrict-authd-frontend/logs:/app/logs
    networks:
      - shenma
