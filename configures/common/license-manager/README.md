# license-manager

license-manager package component

本目录的内容会被 build-components.sh 以 zip 方式整体打包为 `license-manager.zip`。

如需针对不同平台提供不同内容，可在 `./configures/<os>/<arch>/license-manager/` 下放置
平台特定版本；未提供平台目录时，回退到本 common 目录。
